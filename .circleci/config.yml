version: 2.1

# Use a package of configuration called an orb.
orbs:
  # Declare a dependency on the welcome-orb
  welcome: circleci/welcome-orb@0.4.1
# Orchestrate or schedule a set of jobs
jobs:
  # print_hello:
  #   docker:
  #     - image: circleci/node:13.8.0  
  #   steps:
  #     - run: echo Sillians
  # print_world:
  #   docker:
  #     - image: circleci/node:13.8.0
  #   steps:
  #     - run: echo San Francisco, California
  save_hello_world_output:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: echo Hello to Sillians World in San Francisco > output.txt
      - persist_to_workspace:
          root: C:\Users\HP\Desktop\Udacity_DevOps_Nanodegree\.circleci
          paths:
            - C:\Users\HP\Desktop\Udacity_DevOps_Nanodegree\.circleci
            - output.txt
reference:
    steps:
        - checkout
        - run: echo output.txt


  print_output_file:
    docker:
      - image:
    steps:
      - run: cat output.txt
      - attach_workspace:
          at: C:\Users\HP\Desktop\Udacity_DevOps_Nanodegree\.circleci
    
  # build:
  #   docker:
  #     - image: cimg/base:2020.01
  #   steps:
  #     - checkout
  #     - run:
  #         name: "echo an env var that is part of our project"
  #         command: |
  #           echo $MY_SILLIANS_VAR

workflows:
  # Name the workflow "welcome"
  welcome:
    # Run the welcome/run job in its own container
    jobs:
      - welcome/run
      - save_hello_world_output
      - print_output_file:
          requires:
            - save_hello_world_output
      # - build:
      #     requires:
      #       - print_world